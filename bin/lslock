#!/usr/bin/env bash
jar="$1"
java -cp $jar com.github.sakserv.lslock.cli.LockListerCli -d /tmp/lslock-test

#i=10
#while [ $i -gt 0 ]; do
#    echo -e "\nListing Locks: "
#   cat /proc/locks
#   sleep 5
#   i=$(( i - 1 ))
#done